package com.example.quoteapp

import android.content.Context
import com.example.quoteapp.models.Quote

object DataManager {

    val data = emptyArray<Quote>()

    fun readDataFromFile(context: Context) {

    }
}